name: Check communication model
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run model checks
        run: python -m unittest discover -s communication/analysis -p 'test_*.py'
      - name: Recompute data
        run: python communication/analysis/model.py
